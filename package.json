{
  "name": "framework",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node server.js",
    "build": "vite build",
    "build:client": "vite build",
    "build:server": "vite build --outDir dist/server --ssr src/entry-server.js",
    "prerender": "pnpm build:server && node server.js --prerender",
    "preview": "pnpm build:client && pnpm build:server && NODE_ENV=production node server.js",
    "preview:static": "pnpm build:client && pnpm prerender && NODE_ENV=production node server.js",
    "build:ssg": "pnpm build:client && pnpm build:server && node server.js --ssg",
    "preview:ssg": "pnpm build:ssg && vite preview"
  },
  "devDependencies": {
    "vite": "^8.0.1"
  },
  "dependencies": {
    "@chenglou/pretext": "^0.0.5",
    "@react-three/fiber": "^9.5.0",
    "react": "^19.2.5",
    "react-dom": "^19.2.5",
    "three": "^0.183.2"
  }
}
{
  "name": "framework",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun server.js",
    "build": "vite build",
    "build:client": "vite build",
    "build:server": "vite build --outDir dist/server --ssr src/entry-server.js",
    "preview": "pnpm build:client && pnpm build:server && NODE_ENV=production bun server.js"
  },
  "devDependencies": {
    "vite": "^8.0.1"
  },
  "dependencies": {
    "@chenglou/pretext": "^0.0.5",
    "@pmndrs/uikit": "^1.0.64",
    "flubber": "^0.4.2",
    "lenis": "^1.3.23",
    "three": "^0.183.2"
  }
}
